
/**
 * This function adds two numbers together.
 * @param {x} input any number
 * @param {y} input any number
 * @returns {number} that addition of the two numbers
 */
function addTwoNumbers (x, y) {
  return x + y
}

module.exports = addTwoNumbers
